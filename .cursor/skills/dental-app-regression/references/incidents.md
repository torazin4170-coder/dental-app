# インシデント記録（回帰再発防止）

新しい重大不具合を直したら、1件追加する。AI は push 前にこのファイルを読み、触ったコードと照合する。

---

## 2026-09 情報共有シート — テキスト修飾

| 項目 | 内容 |
|------|------|
| **症状** | 中央プレビューで枠線が付かない／赤字が一部だけ。左サイドバーでは強調ボタンが消える |
| **原因** | ① `.iss-doc` に強調CSSが無く DOM 上は付いているが見えない ② `innerHTML` 変更は `input` を発火せず左→プレビュー同期漏れ ③ `issRefreshPreview` がプレビュー編集中にフォーム内容で上書き ④ 強調バーがフォーム上部にありスクロールで見えなくなる |
| **修正** | `fbe483e` — iss-doc CSS、rptEmSyncIssAfterEdit_、issMergeIssFormMetaToContext_、sticky バー |
| **再発条件** | `rptEm*` / `issRefreshPreview` / `issCollectFormToContext_` / AppBody の `.iss-doc` CSS を変更したとき |
| **必須確認** | ISS 左（内容・メモ）＋中央プレビュー、4強調＋解除、改行跨ぎ1ケース |

---

## 2026-09 写真非表示

| 項目 | 内容 |
|------|------|
| **症状** | 診療記録の写真が壊れたアイコンになる |
| **原因** | `getPhotos` 高速化後、`patientPhotoDisplaySrc_` が非公開 Drive を直URLで開いていた |
| **修正** | `f366b76` — driveUrl（GAS `?driveimg=`）を優先 |
| **再発条件** | `getPhotos` / `patientPhotoDisplaySrc_` / 写真表示まわりを変更したとき |
| **必須確認** | 患者1人選び → 写真1枚以上表示 |

---

## 2026-09 起動同期タイムアウト

| 項目 | 内容 |
|------|------|
| **症状** | GAS 再デプロイ後、起動時に最新データが取れない |
| **原因** | UI 45秒制限 vs `getInitData` 直列4シート読込 |
| **修正** | `7d89290` — 4 API 並列、UI 120秒、`readTreatmentBootRecords_` 軽量化 |
| **再発条件** | `getInitData` / `gasCall` タイムアウト / 起動 `init()` を変更したとき |
| **必須確認** | ハードリロード → 施設・患者リストが埋まる |

---

## 2026-09 保存二重化・タイムアウト後ボタン復帰

| 項目 | 内容 |
|------|------|
| **症状** | 保存が遅い／二重保存／タイムアウト後に再押下できる |
| **原因** | `finally` で先にボタン有効化、`handleTreatmentSaveError_` が Promise を返していなかった |
| **修正** | `03273ae` |
| **再発条件** | 診療記録保存フロー・デバウンス・ボタン lock を変更したとき |
| **必須確認** | 保存1回 → 成功トースト → 再表示で内容一致 |

---

## 2026-09 施設月次 — 初診二重表示

| 項目 | 内容 |
|------|------|
| **症状** | 施設月次報告書で「初診」が二重に出る |
| **原因** | `formatTreatmentTextForReportSansShoshin_` の適用漏れ |
| **修正** | `876c131` |
| **再発条件** | 施設月次・初診マーク・帳票テキスト整形を変更したとき |
| **必須確認** | 施設月次プレビューで初診1回のみ |
